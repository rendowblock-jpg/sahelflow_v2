# SahelFlow — Whole-App Transformation Register

> **Status:** Active transformation register
> **Opened:** 2026-09-09 against protected `main` `406debe` (Internal.36 / FD-059 published;
> Founder-installed Internal.36)
> **Authority:** `product/INTERFACE_SYSTEM.md` (the system every surface conforms to),
> subordinate to Product, Experience, Architecture and Founder decisions.
> **Scope:** the Founder directive of 2026-09-09 — *"not just UI and design work, it's full
> functionality and design and structure and enhancing across the full app… transform
> anything that is wrong anywhere it is."*

Every row is a demonstrated finding with an exact evidence pointer, verified in this
checkout. Nothing here is speculative. Rows are `OPEN`, `IN PROGRESS`, `DONE (source, <ref>)`
or `BLOCKED (<reason>)`.

**Evidence discipline is unchanged.** Source conformance never claims installed, Founder,
live-provider, customer, Beta or Stable truth. Rows convert per the existing rules in
`AGENTS.md` and `WORKFLOW.md`.

---

## What the audit found to be sound

Recorded so the transformation does not "fix" what is already right:

- **RTL discipline** — 283 logical-property utilities, zero physical-direction hazards in
  `src/components`. Keep as-is and preserve by construction.
- **Route boundaries** — all 30 dashboard routes carry both `loading.tsx` and `error.tsx`.
- **Locale parity** — `ar.json` / `fr.json` / `en.json` at exactly 2,838 lines each; no
  hardcoded user-facing strings found in components.
- **API authority** — 178 of 193 route handlers carry an auth guard; 187 use the shared
  `withErrorHandler`. The 15 without guards are legitimately pre-auth (auth endpoints,
  health, public storefront checkout, internal runtime IPC, provider status callback with
  constant-time secret comparison). `/api/search` resolves per-permission gating inside
  `universal-search-server.ts`.
- **Domain layer** — durable outbox, proposal-bound AI actions, append-only money truth,
  native containment. The transformation stays above this layer and does not weaken it.

---

## P0 — System foundations (everything else inherits these)

| ID | Finding | Evidence | Status |
|---|---|---|---|
| SYS-01 | **Shared class names merge across stylesheets.** Ownership of *tokens* is deliberately partitioned and documented (globals: radius/status/shadow/elevation; product-system: light surfaces/primary/charts; theme-preset-system: dark + presets) — that part is sound. The defect is that the same **class** is defined in two files at identical specificity, so the effective style exists in neither: `.card-hover` takes its hover `box-shadow` from `globals.css` and its `transform`/timing from `product-system.css`; `.animate-fade-in` takes `animation-name` from the shorthand in `globals.css` and duration/easing from longhands in `product-system.css`. Ten stylesheets, resolved by import order. | `globals.css` vs `product-system.css`: `.card-hover`, `.animate-fade-in`, `.animate-in`, `.animate-scale-in`, `.animate-slide-right`; `src/app/layout.tsx:4-13` | **IN PROGRESS (W1)** — the merging classes proved to be **dead code**: `.card-hover` and `.card-lift` are referenced by **zero** components. 13 rules removed across `globals.css` / `product-system.css` / `motion-system.css`, postcss-verified as removal-only (no rule changed layer, none added). Residue tracked as SYS-10. |
| SYS-02 | **No typographic hierarchy.** 1,200 of 1,291 type-size usages are `text-xs`/`text-sm`/`text-2xs`; only 34 exceed `text-base`. Every screen reads as a dense admin panel. | `grep` over `src/components`: `text-xs` 526, `text-sm` 524, `text-2xs` 150, `text-lg` 11, `text-xl` 10, `text-2xl` 8, `text-3xl` 3, `text-4xl` 2 | **IN PROGRESS (W1)** — the ramp authority now exists in `globals.css` `@theme` (`--text-display` → `--text-caption`, weight and tracking carried with the size). Additive; surfaces adopt it as they are rebuilt. |
| SYS-03 | **11px microcopy floor in Latin locales.** `--text-2xs: 0.6875rem` used 150 times. `arabic-system.css:36` lifts it to 12px, but only under `html[dir="rtl"] [data-sahelflow-shell="desktop"]` — so Arabic is correct inside the dashboard and falls back to 11px on login/setup/join/storefront, while **French and English render 11px at all 150 sites**. | `src/app/globals.css:78`; `src/app/arabic-system.css:36`; `dashboard-layout.tsx:139` | **DONE (source, W5)** — 148 of the 150 call sites migrated: the five AI surfaces the Founder named (`ai-decision-canvas` 15, `ai-work-history` 10, `ai-review-evidence` 7, `ai-tool-result-card` 4, `ai-action-proposal-card` 3) now use `text-caption`. `voice-note-player` keeps `text-2xs` because `voice-note-player.test.ts:123` pins it. **Arabic renders identically** — `arabic-system.css` already lifted `.text-2xs` to 12px inside the dashboard shell — so only French and English gain the 1px. The Arabic one-step ladder was extended to the ramp (`text-caption`/`text-body-sm`/`text-body`) so adopting it cannot silently strip the Arabic reading floor, and `--text-caption` no longer carries `font-weight: 500`, which would have emboldened every timestamp that adopted it. The inbox slice adds 45 sites across 9 components plus one pre-existing `text-[11px]` on destructive error copy in `inbox-v3-queue.tsx:1297` — error text was rendering below the floor precisely where legibility matters most. The remaining 64 sites across 15 components (storefront studio 18, command palette 9, notification center 7, storefront renderer 7, release history 5, automation builder 4, topbar 4, and a tail) migrated in one slice. **`text-[9px]`/`text-[10px]`/`text-[11px]` are now fully retired from `src/components`.** Only `voice-note-player` keeps `text-2xs`, because `voice-note-player.test.ts:123` pins it — the token itself stays defined for that one consumer. **Self-correction:** the `inbox-v3-thread.tsx:1724` unread badge was first left alone over a two-digit overflow concern; that was wrong. It is the identical pattern to the sidebar badge migrated in the same slice — `min-w-4` is a *minimum* with `px-1` and `rounded-full`, so both grow with content. Leaving it would have shipped two sizes for the same UI role. |
| SYS-04 | **2,099 arbitrary values** bypassing tokens that already exist (elevation scale, content widths, rail widths, z-index scale, status colors, `--control-height` are all defined and ignored). | `grep -rhoE '\[[^]]+\]' src/components` → 2,099 | OPEN |
| SYS-05 | **8 corner radii in active use** — `lg` 201×, `md` 171×, `full` 170×, `xl` 90×, `2xl` 24×, `sm` 13×, `none` 2×, `3xl` 2×. | `grep` over `src/components` | **DONE (source, W1)** — 582 substitutions across 137 files collapse xs/sm/md/lg/xl/2xl/3xl and the bare `rounded` onto `rounded-control` (6px) / `rounded-surface` (10px); `rounded-full` and `rounded-none` retained per §3. Verified contract-free before editing (3 pins found, all restated — see TEST-01). Storefront theme radius carved out as seller-owned data (`--radius-storefront-soft/-round`), because collapsing it would have made two of the seller's three theme options render identically. |
| SYS-06 | **20+ unauthorized alpha tints** — `bg-primary/[0.025]`, `/[0.04]`, `/[0.055]`, `/[0.06]`, `/[0.07]`, `/[0.09]`, `bg-warning/[0.035]`, `/[0.09]`, and more. No token authorizes any of them. | `grep -rhoE '(bg\|border\|text\|from\|to)-[a-z-]+/\[0\.[0-9]+\]'` | **DONE for the wash band (W6)** — 15 tint tokens added (`subtle` 4%/`soft` 8%/`strong` 14% × primary/success/warning/destructive/info) via `color-mix`, so each tint tracks the active preset and mode. **223 substitutions across 72 files** collapsed **55 distinct alpha values to 15 tokens**; `grep` for the wash pattern now returns zero outside tests. Mapping was banded, not per-site: alpha ≤ 5.5% → `subtle`, 6–11% → `soft`, 12–19% → `strong`. Verified contract-free **before** the edit — no test file, script or e2e spec asserted any of the 55 strings — then `tsc --noEmit` clean and **880 tests across 132 files green** in `src/components` + `src/app`. **Scope deliberately bounded to background fills on semantic colors below 20% alpha.** Three bands are left OPEN and are *not* covered by these tokens: (a) **border tints** (`border-primary/25`, `border-warning/25`, `border-destructive/30`, …) — hairlines need more alpha than fills, so the 3-step fill scale would misrepresent them; (b) **scrim/overlay alphas** on `muted`/`background`/`foreground`/`accent`/`secondary` (`bg-muted/50` 36×, `bg-background/80` 8×, …) — a different semantic with no token authority yet; (c) **solid-color alphas ≥ 20%** (`bg-destructive/90`, `bg-primary/55`, …), which are opacity on a solid, not a wash. Each needs its own token band before migration. |
| SYS-07 | **No primitive layer.** shadcn atoms, then 600–2,235-line page components, nothing between. Zero `SettingsRow`/`Field`/`Section` primitives exist. | `ls src/components/ui` (27 stock atoms); `grep -rl 'SettingsRow\|SettingsField\|SettingsSection'` → 0 | **IN PROGRESS (W2)** — `src/components/system/` created as the primitive namespace with `PageShell`, `Section`/`SectionStack`, `Row`/`RowGroup`, `Field`, `Panel`, `IconTile`, `Toolbar`, and a barrel that promotes `StateSurface` as the canonical state primitive. Additive: no existing surface imports it yet, so the wave cannot regress a page. `Rail` and `DataTable` deferred — `Rail`'s disclosure contract is defined by the Agents rebuild (W3), and `DataTable` needs its own design pass against the existing per-domain tables. |
| SYS-08 | **The tinted icon tile is hand-rolled 36 times** at differing sizes, radii and tints — highest density in `ai-decision-canvas` (10), `automation-builder` (9), `inbox-v3-thread` (7), `command-palette` (7), `ai-review-evidence` (7). | `grep` over `src/components` | **DONE (source, W2)** — the primitive was the blocker, not the call sites. IconTile shipped offering size-7/size-9 on the control radius and size-11 on the surface radius and was adopted **zero** times, because no real tile had those proportions: the hand-rolled tiles use size-6/8/9/10/11/12, most sit on the surface radius, and most have no border at all. The scale was corrected to the sizes that actually occur (xs/sm/md/lg/xl) and `border` made opt-in; 16 tiles across 13 files then converted cleanly. The 3 remaining `rounded-full` instances (`ai-review-evidence`, `ai-work-history`, `feature-gate`) are circular empty-state marks, a different concept — they belong to UI-07/EmptyState, not here. |

| SYS-09 | **App CSS overrides the Radix overlay animation system.** `.animate-in` is provided by `tw-animate-css` (`globals.css:2`) and used by every Radix overlay — tooltip, dropdown, dialog, alert-dialog, sheet, popover, select. It is then redefined twice locally: `globals.css` sets the full `animation` shorthand to a local `animateIn` keyframe inside `@layer utilities`, and `product-system.css` forces `animation-duration`/`animation-timing-function` unlayered. Unlayered wins over layered, so overlay enter animations run on app timing, and the local shorthand may be displacing Radix's own `enter` keyframe. **Requires empirical verification in the running app** before any change — the cascade cannot be settled by reading alone. | `src/app/globals.css:2`, `:310`; `src/app/product-system.css:301`; consumers in `src/components/ui/{tooltip,dropdown-menu,dialog,alert-dialog,sheet}.tsx` | OPEN |
| SYS-10 | **Dead-CSS residue after the W1 pass.** Rules that survive because they sit inside `@layer` blocks or in grouped selectors shared with live classes: `globals.css:320` `.animate-scale-in`, `:325` `.animate-slide-right`, `:496-507` `.card-hover` (+ its `.dark` variant), `:617-632` `.force-ltr`/`.numeric-value`, `:857` `.theme-transition *`; `motion-system.css:75,82,88,93,98,208` `:is(.card-lift, .card-hover)` groups now referencing base classes that no longer exist; `product-system.css:203-212` `.bidi-data`/`.bidi-copy`. `product-system.css:197` must be preserved — its group also carries the **live** `.technical-value`. Requires per-selector judgment, not a mechanical pass. | listed line pointers | OPEN |

| TEST-01 | **The test suite pins the defects as law, so the interface cannot be improved — only added to.** Of 444 vitest files under `src/`, **136 assert on source text** through `readFileSync`, carrying **3,980 `toContain` assertions**, while **3** actually render a component (the harness PR #411 added). The Agents layout defects in this register are *literally encoded as contract*: `ai-workspace-contract.test.ts:25` pins `useMediaQuery("(min-width: 1500px)")` (IA-04), `:26`/`:28` pin the exact grid strings `grid-cols-[17.5rem_minmax(0,1fr)]` and `grid-cols-[17.5rem_minmax(0,1fr)_20rem]` (IA-03), `wave2-ai-composition-contract.test.ts:19-31` repeats them, and `ai-final-review-contract.test.ts:74` pins a multi-line source snippet including its indentation. The test named *"uses two panes at common desktop width and progressive review evidence"* asserts only that three literal Tailwind strings exist in a file — **it would pass if the component rendered nothing.** PR #411 demonstrated the same class of failure directly: pinning `visibleStart = 0` disabled INB-11 virtualization completely and 130 tests still reported green, including the suite whose only job was to prove virtualization worked. This is why F-06 and F-12 could only repaint: structure was frozen, so presentation was the only thing left to change. | `grep -rl readFileSync src --include='*.test.ts*'` → 136; `grep -rl '@testing-library/react\|@/test-utils/render'` → 3; `src/components/ai/__tests__/ai-workspace-contract.test.ts:21-36`; `wave2-ai-composition-contract.test.ts:18-31`; `ai-final-review-contract.test.ts:64-78` | **OPEN — P0.** Every surface rebuild must convert that surface's pinning assertions from source text to behaviour **in the same change**, using the `src/test-utils/render.tsx` harness. The contract is re-expressed, never deleted: "the review surface is reachable at every width" replaces "the file contains this media query". A rebuild that deletes a contract without restating its intent is not done. |

| SEC-01 | **Main's exact head carries no dependency-audit evidence, and it is currently carrying two criticals.** `documentation/README.md` states the only machine-proven exact-head audit truth is the blocking `bun audit --production` check at the exact audited commit. The risk classifier skips the Quality Gate and source-quality diagnostics for docs-only PRs, so **PR #413 — which *is* current `main`** — ran neither. PR #411 (2026-09-08) passed both, so the 8 advisories were published in the interval and are **not introduced by any branch**. Pinned: `next` 16.2.11 (advisory `>=16.0.0 <16.3.3`), `sharp` 0.35.3, `js-yaml` 4.3.1, sidecar `hono`, plus a transitive moderate. | PR #413 vs #411 check runs; PR #414 job 102436786190; `package.json` pins | **OPEN — P0. Reachability assessed 2026-09-09; the network-remote vector is not reachable, the fix is still required.** Evidence: the Next server runs `output: "standalone"` and is spawned by the Tauri shell with `HOSTNAME=127.0.0.1` (`src-tauri/src/lib.rs:692`) on an ephemeral `PORT`; loopback is enforced as an **invariant**, with two independent modules rejecting a manifest whose `host != "127.0.0.1"` (`installation_root_rotation.rs:242`, `survivability_controller.rs:215`); the port is reserved by binding `("127.0.0.1", 0)` (`child_containment.rs:980`); `runtime_protocol.rs:9` fixes `LOOPBACK_HOST`. **There is no listening socket on a routable interface, so `GHSA-p293-qw3h-jr36` has no network-remote path.** For `GHSA-2xp9-vwfh-vxw4` (Image Optimization / AVIF), `next/image` is used in exactly one component (`brand/sahelflow-mark.tsx`) and `next.config.ts` declares no `images` block, so no `remotePatterns` are configured and the optimizer refuses remote sources by default. **What this does NOT establish:** whether either advisory is exploitable *locally* — by another process on the machine reaching loopback, or by DNS-rebinding/CSRF against the loopback port from a page the seller visits. That needs the advisory mechanics and a real test. The honest disposition is **mitigated, not unaffected**: severity for this deployment shape is materially reduced, and the dependency bump is still the fix. It is not performed here because the lockfile cannot be regenerated in this environment (`bun install` is unavailable) and a framework bump does not belong in a design-system PR. **Blocker narrowed (W6, 2026-09-09).** The exact fixed versions were confirmed to exist on the registry, which is reachable from the agent container: `next` 16.3.4 (advisory needs >=16.3.3), `sharp` 0.35.4, `js-yaml` 4.3.2, `hono` 4.13.7 (needs >=4.13.5), `baseline-browser-mapping` 2.11.21 (needs >=2.11.0). The edit is small and follows the repo's existing convention — `package.json` already pins transitive deps through an `overrides` block, so `js-yaml`, `sharp` and a new `baseline-browser-mapping` pin all belong there, with `next` and `sharp` in `dependencies` and `hono`/`sharp` in `sidecars/whatsapp/package.json`. **What is still missing is only the lockfile.** `bun install` could not be completed in this container, and CI runs `bun install --frozen-lockfile`, so committing bumped manifests **without** a regenerated `bun.lock` would turn one red check into a hard install failure. The manifest edit was therefore made, verified, and reverted rather than pushed. **RESOLVED (verified 2026-09-12 against protected `main` `cd1114d`).** Every pin this row called missing is present and the lockfile was regenerated: `next` 16.3.4 and `sharp` 0.35.4 in `dependencies`, `sharp` 0.35.4 / `js-yaml` 4.3.2 / `baseline-browser-mapping` 2.11.21 in `overrides`, `hono` ^4.13.5 and `sharp` 0.35.4 in `sidecars/whatsapp/package.json`, and `bun.lock` committed alongside them. The row is kept rather than deleted because its reachability analysis (loopback-only invariant, single `next/image` call site, no configured `remotePatterns`) remains the standing disposition for this deployment shape. **The advisory surface must be re-measured per package action — a fixed version today is not evidence for a future head.** |

| SEC-02 | **The declared 80% coverage floor is not enforced, and is not being met.** `vitest.config.ts:52-59` sets `statements: 80` / `lines: 80` with the comment *"Maintain the 80% application-source floor"*, but `.github/workflows/ci.yml:178-180` runs the coverage step with `continue-on-error: true`, so the threshold logs `ERROR` and the job proceeds. Measured on PR #414: **77.28% lines and statements** against the declared 80% floor. Because the step never blocks, this can have been true for an unknown number of merges without anyone seeing it — PR #411's Quality Gate reports `success` and would have done so regardless of its coverage number. Coverage instruments `src/lib/**/*.ts` only, so component work does not affect it either way. | `vitest.config.ts:40-59`; `.github/workflows/ci.yml:177-184`; PR #414 Quality Gate job 102441082016 | **DONE (source)** — resolved as a contradiction between two authorities, not a number: `vitest.config.ts` declared an 80% floor while `ci.yml:177` ran coverage under `continue-on-error: true`, so the floor could not fail and `vitest run --coverage` failed locally on a tree CI called green. Thresholds pinned to measured truth (77.29% stmts/lines, 76.79% branch, 83.02% funcs -> 77/76/82/77) and the step made blocking, converting a decorative number into an enforced regression ratchet. |

## P0 — Information architecture

| ID | Finding | Evidence | Status |
|---|---|---|---|
| IA-01 | **Two page architectures.** 21 routes use `PageHeader`; Agents and Inbox opt out entirely and hide their `<h1>` with `sr-only`, so those screens carry no visible identity. | `src/app/(dashboard)/agents/page.tsx:32`, `inbox/page.tsx`; `PageHeader` adoption = 21 routes | **DONE (source, W3/W5)** — both opted-out routes are on `PageShell variant="workspace"`, so Agents and Inbox carry visible `<h1>`s while keeping full-height geometry (`app-workspace-content` stays on each route root for the `#main-content:has(> …)` rule). The deeper half is also closed: `PageShell` and `PageHeader` each rendered their OWN header, so the product had two headings with two type scales. `PageHeader` is now the single implementation and `PageShell` composes it — and its heading joined the type ramp, having previously hardcoded `text-xl`/`sm:text-2xl` with an arbitrary `text-[15px]` description on 21 routes. Three contract pins converted, none deleted. |
| IA-02 | **The navigation model is built and then discarded.** `navigation.ts` defines 8 semantic domains with parent/child relationships; `sellerSidebarNavigationItems` flattens them to 16 equal-weight items with no grouping — the code comment states the hierarchy is deliberately dropped at render. | `src/components/layout/navigation.ts`; `founder-rtl-nav-demo-year-contract.test.ts:46-59`; `product/DECISIONS.md:665` | **CORRECTED — deliberate.** The flattening is a recorded product decision, not drift: the contract test is titled "uses one fixed seller-priority sidebar and **retires custom reordering**" and asserts `ui-store` no longer carries `navigationDomainOrder`/`setNavigationDomainOrder`, while `SF16-NAV-017` shows seller reordering was an earlier item that was deliberately retired. The domain model is deliberately preserved for route context, search and permissions while the *visible* order is owned by SahelFlow — the code comment states this explicitly. What remains legitimately open is narrower and is a design question, not a defect: **16 destinations render at equal weight with no visual grouping or rhythm**. |
| IA-03 | **The Agents layout premise is wrong.** Four columns of chrome (nav 17.5rem + history rail 17.5rem + canvas + review rail 20rem) leave the conversation ~45% of a 1600px screen, while both flanking rails routinely render empty. | `src/components/ai/ai-decision-workspace.tsx:150`; `e2e/ai-workspace.spec.ts:127-138` | **CORRECTED — partly by design; residual reviewed in W3.** The three-column layout at >=1500px is deliberate and verified behaviourally (`e2e/ai-workspace.spec.ts:127`), not drift. The demonstrated defect was the rail rendering empty scaffolding, closed as UI-01. W3 reviewed the remaining question (a reading measure on the canvas at very wide viewports) and left the layout untouched: it is contract-locked, behaviourally covered at 1600px, and changing it is a design decision for the Founder, not a repair. |
| IA-04 | **Layout branches in JavaScript at hardcoded pixels** — `1500px`, `1280px`, `1024px` — so the product has a different information architecture per monitor. The Agents review rail exists only above 1500px. | `ai-decision-workspace.tsx:28`; `e2e/ai-workspace.spec.ts:121-138`; `ai-workspace-contract.test.ts:25` | **CORRECTED — deliberate, and contract-locked.** The 1500px branch is an intentional design decision with behavioural e2e coverage at 1600px, and it is additionally pinned as source text by `ai-workspace-contract.test.ts:25` and `wave2-ai-composition-contract.test.ts:31` (TEST-01). Changing it is a design change requiring the e2e expectation to change with it — not a defect repair. Reclassified from defect to open design question for the Agents rebuild. |
| IA-05 | **Settings form controls are not labelled.** Across 13 panels there are **24 `<Input>` elements and zero `aria-describedby`** — no hint text anywhere in Settings is programmatically associated with its field. Worse, `collaboration-admin-panel` carried **5 inputs and 2 selects with no `<Label>` and no `aria-label`, identified only by `placeholder`**, which fails WCAG 3.3.2 and 4.1.2: the accessible name disappears the moment the seller types. | `collaboration-admin-panel.tsx:279-280,430-443`; `grep -c aria-describedby src/components/settings/*.tsx` → 0 across all 13 | **DONE (source, W3/W4)** — closed in two passes. First: all 6 re-authentication fields link their existing visible description via `aria-describedby` (0 -> 6), the 3 with an error path announce it and set `aria-invalid`, and `team-access-authority-panel` gained the accessible name it lacked. Second pass found 4 inputs with no association at all, every one in a security-critical flow: `ai-key-panel` PIN (placeholder only — a placeholder is not an accessible name and vanishes on first keystroke); `danger-zone-panel` reset confirmation (a `<Label>` with no `htmlFor` and an `<Input>` with no `id`, on the control that gates a database reset); `delivery-credentials-panel` PIN (no name at all); and `backup-restore-panel` recovery code, where ONE `<label>` wrapped both the field and its hint, so the accessible name was the label text with the whole hint sentence concatenated onto it. Settings now has zero unlabelled inputs and 8 `aria-describedby` links. Per IA-05b the Card->Panel migration is deliberately NOT done: it would have given panels borders and radii and made the surface more island-like, not less. |
| IA-05b | **CORRECTED — the "card islands" reading was wrong.** I claimed Settings renders as 11 bordered card islands. It does not. `settings-control-center.module.css` sets `border: 0; border-radius: 0; background: transparent; box-shadow: none` on every `[data-slot="card"]` inside `.stack`, and `.stack > * + *` adds a `border-top`, so Settings **already renders as flat sections separated by hairlines** — a reasonable settings grammar. The card wrapper is a code-structure artifact, not a visual one. Consequently the migration I proposed (swap `Card` → `Panel`) would have **given panels borders and radii and made the surface more island-like, not less**, and the contract conversion I asked the Founder to authorise was unnecessary for it. The convoluted mechanism (add card chrome, then strip it in CSS) is real but cosmetic-internal and low value to change. | `settings-control-center.module.css:36-48`; `settings-visual-system-contract.test.ts:44-58` | **CORRECTED — no action.** W4 is not contract-blocked; the valuable Settings work is IA-05 (labelling), which needs no contract change. |

## P1 — Surface defects

| ID | Finding | Evidence | Status |
|---|---|---|---|
| UI-01 | **Empty rails present three stacked cards that each announce emptiness**, with the same explanatory sentence repeated. A third of the Agents screen is furniture apologising for having no content. | `ai-review-evidence.tsx`; `src/lib/i18n/ai-decision-workspace.ts:151` | **DONE (source, W3a)** — the three slices now resolve to one `reviewEmpty` verdict and the rail renders a single `StateSurface` (the promoted system primitive) instead of three stacked dashed boxes, two of which printed the same sentence. Errors and loading are excluded from the verdict, so a failure still reads as a failure. The provider/privacy section below is unchanged — it carries real state. |
| UI-02 | **Debug artifacts reach the seller** — tool-result cards render internal result counts (`النتيجة · 0`) as user-facing content. | `ai-tool-result-card.tsx:437` | **DONE (source, W3a)** — the count renders only when there is something to count. A succeeded tool no longer prints an internal array length of 0 on the seller's screen; the empty case falls through to the existing truthful no-records line. |
| UI-03 | **Permanent keyboard-shortcut ribbon** under the Agents composer (4 shortcuts always visible) instead of on-demand disclosure. | `ai-decision-canvas.tsx` composer footer | **DONE (source, W3)** — the four shortcuts move from a permanent row under the composer into a native `<details>` disclosure, collapsed by default. `<summary>` gives a real keyboard- and screen-reader-operable trigger with expanded state announced for free, and the label reuses the existing `common.cheatsheet` key, so no new copy was introduced in any locale. |
| UI-04 | **Duplicate status indicator** — "setup ready" renders twice on one Agents screen. | Agents canvas + review rail, both bound to `setup.ready` | **DONE (source, W3)** — confirmed: the avatar dot and the labelled config chip in the same header are both bound to `setupReady`, ~40 lines apart, so above `sm` the screen stated one fact twice — once in words, once as an unlabelled dot. The chip is `hidden sm:inline-flex`, so the dot is now scoped `sm:hidden`: exactly one indicator at every viewport and never zero. Below `sm` the dot also gained the accessible name it never had (a bare colour is not a signal to assistive tech). |
| UI-05 | **Session cards dump a raw prompt excerpt** (`line-clamp-2` of the message body) instead of a meaningful title and metadata line; reads as clipped text in RTL. | `ai-work-history.tsx:517` | **DONE (source, W3)** — the register's reading was close but not exact: the card does render a title, and titles *are* derived. The real defect was the derivation — `message.slice(0, 50)` inlined in both message routes, which cut mid-word, could cut through a surrogate pair (storing a lone surrogate), and kept the raw newlines of a pasted prompt. Replaced by `deriveAiSessionTitle` (word-boundary, code-point-safe, whitespace-collapsed, 12 unit tests incl. Arabic and astral input). Second half: the title comes from the session's FIRST message and the preview shows its LATEST, so a single-question session printed the seller's prompt twice — the duplicate preview line is now suppressed. |
| UI-06 | **24 of 30 loading states are one of two identical generic full-page skeletons** that cannot mirror the page they stand in for. | `md5sum` over `src/app/(dashboard)/**/loading.tsx`; `full-page-skeleton.tsx`; `algerian-demo-contract.test.ts:277` | **DONE (source, W5)** — measured rather than assumed, and the register's framing was too broad. `FullPageSkeleton` is already parameterised (`showStats`/`showTable`/`statCount`/`rowCount`); the defect was every route taking bare defaults. Resolving each route's real shape through its delegated components showed the default is CORRECT for 11 of the 16 list routes. Four were lying and are fixed: `automations` (no table), `imports` (no stats), `notifications` (list only), `storefronts` (card grid, no table). A skeleton that promises a table the page never renders is the defect; identical-but-accurate skeletons are not. |
| UI-07 | **Four competing state primitives**, one of them dead: `state-surface` (19 uses), `empty-states` (11 hardcoded per-domain copies), `empty-state` (2 uses), `operational-state` (**0 uses**). | `src/components/shared/` | **CORRECTED — largely resolved by construction; do NOT "consolidate" further.** The row counted composition layers as competitors. After W2 deleted the dead `operational-state`, what remains is ONE renderer with two pure-composition layers: `StateSurface` (19 direct uses) renders; `EmptyState` (2 direct uses) only composes an action node and delegates every visual decision to StateSurface; `empty-states.tsx` is a per-domain COPY catalog that renders **zero markup of its own** (verified: no `<div>`, `<section>`, `<p>` or `className` in the file) and composes `EmptyState`. That is the intended shape — one renderer, thin adapters — not four implementations. Collapsing them would delete a useful copy catalog and inline domain copy into 5 call sites, which is the same trap IA-05b caught in W4. Residual, and genuinely open: 16 files carry `border-dashed` outside the primitives, but most are drop zones, placeholders and chart affordances rather than empty states — each needs the judgement of the surface that owns it, so it belongs to SYS-04's continuous retirement, not to a sweep here. |
| UI-08 | **Motion system is 235 lines of largely unused ceremony** — real usage is 147 `animate-spin` loading spinners and almost nothing else. | `src/app/motion-system.css`; `grep 'animate-'` over components | OPEN |

| FN-01 | **Sellers cannot create a shop.** The whole stack exists — `POST /api/shops` (guarded by `requireTrustedAction("shops.create")`, enqueuing native provisioning), `shop-store.createShop`, `lib/shops/createShop`, and trilingual copy (`shops.createTitle`, `shops.createDescription`, `shops.created`, `shops.newShop`, "manage up to 10 shops"). The **only** UI that calls it is `layout/create-shop-dialog.tsx`, which **nothing imports**. The topbar shop control only switches (`setActiveShop`, `loadShops`). Multi-shop is a paid entitlement under FD-017 (5,000 DZD per extra shop), so a seller who pays for a second shop has no way to create it. | `src/components/layout/create-shop-dialog.tsx` (0 importers); `src/components/layout/topbar.tsx:87-155`; `src/app/api/shops/route.ts:58-77`; `src/stores/shop-store.ts:45,111`; `src/lib/i18n/locales/en.json:2285-2295` | **DONE (source, W2b)** — `create-shop-dialog.tsx` rebuilt on the system primitives (controlled, `Field`-wired, icon picker given real radiogroup semantics instead of ten unlabelled buttons) and reachable from the topbar workspace switcher. `onSelect` is prevented so the closing menu cannot unmount the dialog. Slot authority is not re-implemented client-side; the native refusal is surfaced verbatim through `translateServerError`. |
| FN-02 | **Entitlement enforcement for extra shops is unverified.** `POST /api/shops` enforces the `shops.create` permission but the paid-entitlement and 10-shop ceiling are asserted in seller-facing copy; where (or whether) they are enforced was not traced. Must be settled before FN-01 is wired, so the repair cannot hand away a paid feature. | `src/app/api/shops/route.ts:58-77`; `src/lib/license/lifecycle-authority.ts:109-142`; `src-tauri/src/shop_lifecycle_mutation_02.inc.rs:30`; `src-tauri/src/shop_lifecycle.rs:216` | **RESOLVED — no defect (W2b).** Enforcement is in the native layer: `shop_lifecycle_mutation_02.inc.rs:30` refuses creation once `registry.shops.len() >= shop_slots`, against the **signed** slot count, and `shop_lifecycle.rs:216` range-checks the signed value to 1..=10. `requireLifecycleEntitlementAuthority` additionally rejects a projection that disagrees with the signed lifecycle envelope. Wiring the UI cannot hand away a paid shop. |

| FN-03 | **Five of the eight shop-lifecycle actions have no UI at all.** `renameShop`, `archiveShop`, `recoverShop`, `removeShop` and `loadArchives` have **zero** references outside the store. Each has a guarded API route (`PATCH`/`DELETE /api/shops/[id]`, `POST /api/shops/[id]/archive`, `GET /api/shops/archives`, `POST /api/shops/archives/[archiveId]/recover`), a native Rust lifecycle mutation and signed entitlement authority behind it. A seller can switch shops and — after FN-01 — create one, but cannot rename, archive, recover or delete one. | `src/stores/shop-store.ts:117-150`; the four route files; `src-tauri/src/shop_lifecycle_mutation_*.inc.rs` | **OPEN — deliberately deferred.** These operations mutate and destroy shop *databases*, and the native path cannot be exercised in a container (it requires Tauri). Building destructive shop administration without being able to run it would violate the preservation rule in `AGENTS.md`. Build it as its own reviewed surface, on `RowGroup`/`Row`, once it can be validated. |
| SYS-11 | **W1 removed a rule pinned by the motion contract.** `motion-system-contract.test.ts:41` asserts `.animate-scale-in` exists in `motion-system.css`; the W1 dead-CSS pass deleted it along with `sf-surface-scale`. The class has zero component consumers, but it is declared motion vocabulary under an accepted contract, and this machine cannot run vitest to catch it. | `src/components/shared/__tests__/motion-system-contract.test.ts:41`; commit `a447245` | **DONE (source, W2b)** — restored in `motion-system.css` with its keyframes, per the repo rule that contract tests are design law and adaptation goes code-side. Because that file is imported last, it is now the **single owner** of `.animate-fade-in` / `.animate-scale-in`: the partial longhand overrides from `globals.css` and `product-system.css` stay deleted, which resolves the SYS-01 merge for this vocabulary. Retiring the vocabulary belongs to UI-08, as one change with its contract. |

## P1 — Content and localization

| ID | Finding | Evidence | Status |
|---|---|---|---|
| L10N-01 | **Two parallel copy systems.** The 3 JSON locale files (2,838 lines each, at parity) plus **39 inline TypeScript translation modules totalling 5,979 lines** holding their own AR/FR/EN maps — more copy outside the copy authority than inside it. This is the mechanism by which wording drifts between screens. | `src/lib/i18n/*.ts` (39 modules) vs `src/lib/i18n/locales/*.json` | OPEN |
| L10N-02 | **Demo/seed content is hardcoded French** and renders inside an Arabic interface. Not a rendering bug — the seed is not locale-aware. | `src/lib/demo/algerian-demo.ts:835`, `:840` | **DONE (source, W3a)** — narrowed by evidence, then fixed. Seller- and customer-authored demo content (product names, WhatsApp messages) stays French/Arabic/Darija mixed **deliberately**: that is what an Algerian seller's real day looks like, and flattening it would make the demo less honest. The defect was the demo **AI transcript**, which is SahelFlow's own voice answering in French inside an Arabic interface. `DEMO_AI_SESSION` now carries the title and four turns in AR/FR/EN, `seedAlgerianDemoData` and `loadAlgerianDemoWorkspace` take an optional `locale` defaulting to `fr` (so all 14 existing callers and every test are unchanged), and `POST /api/demo-data` resolves the seller's locale through `getI18n()`. |

## P2 — Structure and dead code

| ID | Finding | Evidence | Status |
|---|---|---|---|
| STR-01 | **God components.** `inbox-v3-thread` 2,235 · `storefront-studio` 1,752 · `ai-decision-canvas` 1,727 · `automation-builder` 1,496 · `inbox-v3-queue` 1,325 lines. This is the mechanism that re-derives patterns inside a single file. | `wc -l` over `src/components` | **IN PROGRESS (W3/W7) — `ai-decision-canvas` is closed; the other four are untouched.** The canvas went **1,767 → 964 → 355 lines** across five extractions, every one a verbatim move with behaviour unchanged, and now sits under the §13 400-line rule. W3 landed `ai-message-bubble.tsx` (259) and `ai-start-surface.tsx` (602 — setup/error notices, abilities panel, inbox strip, starters). W7 landed the three body seams the earlier note correctly called "a genuine decomposition rather than a move": `ai-composer-deck.tsx` (PR #416 — composer, attachment ingest, screenshot pipeline, shortcut disclosure), `ai-message-log.tsx` (PR #418 — transcript list, skeleton, load-earlier, regenerate, inline proposals) and `src/hooks/use-ai-canvas-shortcuts.ts` (PR #419 — the AI-22 window keydown surface). Pin exposure was **measured before each cut, never discovered during it**: all nine AI contract test files were read in full, and of 98 `expect(canvas)` assertions, 10 were MessageBubble-only and 21 start-surface-only. Every moved assertion was re-anchored in the same change and **none relaxed** — including an `indexOf` ORDERING assertion whose two operands both moved (a `toContain`-only sweep would have silently left it comparing -1 to -1) and a `messages.length === 0` read in `ai-operational-launchpad-contract.test.ts` that still pointed at the canvas after the log left it, caught and repointed in #418. Wiring assertions (`copy={copy}`, `isLatest=…`, `onFeedback=…`) deliberately stay on the canvas, which is what decides them. `AiCopyFn` moved to `ai-workspace-types.ts` to avoid a circular import. **The canvas header was assessed and deliberately left in place:** `f06-page-completion-contract.test.ts` pins five header strings directly on the canvas under a comment declaring the badge canvas-owned, and `ai-workspace-contract.test.ts` adds `onBack`/`backToSessions`, so extracting it would relax a contract rather than decompose a file. **The row stays open on the remaining four:** `inbox-v3-thread` (85,007 B — the largest source file in the repo, covered by 21 test files), `storefront-studio`, `automation-builder`, `inbox-v3-queue` (50,499 B). The sanctioned shape for those is already in the tree — `src/hooks/inbox/use-inbox-thread.ts` (INB-27) extracted thread projection and tail management into a hook module, exactly the move used for the canvas shortcuts. |
| DEAD-01 | **~1,600 lines of dead UI shipping in the binary** — zero production and zero test imports: `settings/team-access-panel.tsx` (694, abandoned beside its 862-line replacement), `inbox/whatsapp-ingress-recovery-panel.tsx` (297), `shared/premium-table.tsx` (173), `profile/current-identity-card.tsx` (171), `shared/operational-state.tsx` (129), `layout/create-shop-dialog.tsx` (118), `orders/cod-controls.tsx` (50). | verified import scan, prod + test | **DONE (source, W2)** — 6 files / 1,514 lines deleted after a second zero-reference verification at deletion time. **`create-shop-dialog.tsx` deliberately NOT deleted** — it is the missing entry point for a paid feature, tracked as FN-01. |
| DEAD-02 | **Wave-migration leftovers** — `delivery-credentials-panel.tsx` is a 3-line shim while `delivery-credentials-panel-wave3.tsx` is the real implementation. | `src/components/settings/` | **DONE (source, W2)** — not dead code but a live indirection: 2 production importers plus a contract test pinning the path. `delivery-credentials-panel-wave3.tsx` renamed onto the canonical `delivery-credentials-panel.tsx`, export renamed, and the 2 `readFileSync` contract paths re-anchored in the same change. |
| STR-02 | **Type-safety erosion — re-measured 2026-09-12, and much smaller than recorded.** Actual: **205** `as unknown as` casts and **32** non-null assertions outside tests, not 208 and 321. The non-null figure was overstated by roughly ten times. More usefully, **106 of the 205 casts — over half — are in one file**, `src/lib/crypto/with-protected-pii.ts`, where they are a Prisma generics limitation in a single delegate wrapper rather than scattered rot. The remaining ~99 are spread thin (next highest: 9 in `product-service.ts`). This is one concentrated structural problem plus a thin tail, which is a different and far more tractable piece of work than the original wording implies. | `grep` over `src/` excluding `__tests__` and `*.test.*` | **OPEN — rescoped.** Triage the `with-protected-pii.ts` wrapper as its own change; the tail is per-site judgement, not a sweep. |
| STR-03 | **Was: 28 TODO/FIXME/HACK/ts-ignore markers outside tests.** | `grep -rnE '(TODO\|FIXME\|HACK):\|@ts-ignore\|@ts-expect-error'` over `.` excluding `node_modules` | **DONE — verified 2026-09-12: ZERO markers remain.** Not one `TODO`, `FIXME`, `HACK`, `@ts-ignore` or `@ts-expect-error` exists anywhere in the repository — `src/`, `src-tauri/`, `sidecars/`, `scripts/` and `control-plane/`, tests included. The row was closed by earlier work and never marked. |

---

## Execution order

Foundations first, because every surface inherits them; then the two surfaces the Founder
named; then the remainder in waves. Each wave lands green and self-contained.

1. **W1 — Token authority.** SYS-01, SYS-02, SYS-03, SYS-05, SYS-06. Collapse to one
   resolved stylesheet system, establish the type ramp, three radii, semantic tints. Retire
   `--text-2xs`.
2. **W2 — Primitive layer.** SYS-07, SYS-08, UI-07, DEAD-01, DEAD-02. Build `PageShell`,
   `Section`, `Row`, `Field`, `Panel`, `IconTile`, `EmptyState`, `Rail`, `Toolbar`,
   `DataTable`. Delete the dead files and the duplicate state primitives.
3. **W3 — Agents rebuilt.** IA-01, IA-03, IA-04, UI-01..UI-05, STR-01 (canvas). From the
   information architecture up, on the new system.
4. **W4 — Settings rebuilt.** IA-05 on the `Row`/`Section` grammar; 11 card-islands become
   one coherent surface.
5. **W5 — Navigation and page shell.** IA-01 across all 30 routes, IA-02 restored hierarchy,
   UI-06 per-page skeletons.
6. **W6 — Copy authority.** L10N-01 migration, L10N-02 locale-aware seed.
7. **W7 — Structure.** Remaining STR-01 decomposition, STR-02/STR-03 triage. The canvas is
   closed (PRs #416, #418, #419); the four remaining god components are next, `inbox-v3-thread`
   first, on the `use-inbox-thread.ts` hook-extraction precedent.
8. **W8 — Conformance sweep.** Every surface against `INTERFACE_SYSTEM.md` §14, recorded here.

SYS-04 (2,099 arbitrary values) is retired continuously across W1–W8 rather than as one
mechanical pass, so each removal is verified in the surface that owns it.

---

## Conversion rules

1. A row is `DONE (source, <ref>)` only when the named defect is repaired **and** the
   surface passes the `INTERFACE_SYSTEM.md` §14 conformance list.
2. Source completion is never installed, Founder, live-provider or customer evidence.
   Installed conversion follows the existing campaign discipline.
3. A repair that introduces a new arbitrary value, a second primitive for an existing
   concept, or a component over 400 lines is not done.
4. New findings get a new row with an exact evidence pointer. They are never folded
   silently into an existing row.
5. Protected invariants (`AGENTS.md` §Protected boundaries) are never weakened by a
   transformation row. Where a repair would touch one, it stops and is raised.

# SahelFlow working memory

> **Purpose:** Compact execution frontier; never product, architecture or roadmap authority
> **Last updated:** 2026-08-03
> **Live protected main before merge:** `991c61ac882497fdda01af3ac04f06978146bbda`
> **Execution epic:** issue #164
> **Active product phase:** Phase 2 closure transitioning to Phase 3
> **Active package:** PR #200 — native multi-shop authority
> **Exact implementation head:** `42a3cf678405e929e5a63f71e106f954b1e1f7a5`
> **Active implementation agent:** ChatGPT Web Agentic Coding Agent

Live GitHub is authority. Re-read protected `main`, PR #200, its exact head and
issue #164 before relying on copied state.

## Phase 2 closure decision

The Founder explicitly authorized PR #200 to merge on 2026-08-03 so the roadmap
can advance to Phase 3.

The native multi-shop implementation is complete at source level:

- Rust is the sole authority for create, rename, switch, archive, recover and
  destructive delete;
- typed intents, installation-root authentication, revision fencing, signed shop
  slots and exact shop incarnations are enforced;
- the durable lifecycle journal owns runtime quiescence, registry/database
  mutation, compensation and startup reconciliation;
- browser registry mutation, generic relaunch and broad WebView process authority
  are removed;
- archive/delete/recover interruption windows are covered by native integration
  tests on Linux and Windows.

## Exact evidence accepted

The exact implementation head passed:

- TypeScript, ESLint, complete Vitest, Prisma, coverage and dependency audit;
- Rust 1.77 lifecycle tests and strict Clippy;
- real Tauri switch and mutation interruption contracts;
- Linux and Windows release compilation;
- Windows database, standalone runtime, contained launcher, containment stress,
  complete Rust lifecycle suite and strict Clippy;
- ephemeral MSI build plus installed launch, close and reopen.

## Explicit retained limitation

The installed hydrated-WebView proof did **not** pass on the ephemeral GitHub
Windows runner. The MSI installed and launched, but the harness did not observe the
authenticated durable UI-ready receipt twice. The Founder explicitly waived this
single Phase 2 exit gate for merge.

This waiver does not convert the failed check into passing evidence. It does not
claim Founder acceptance, a release, Stable readiness or full installed-UI
certification.

The first Phase 3 package must preserve the installed evidence lane and resolve or
reclassify the WebView2 hydration proof through a focused Windows installed-runtime
investigation. It must not weaken or silently remove the check.

## Phase 3 frontier

After PR #200 merges, the active roadmap phase is:

**Phase 3 — durable providers, inbox, AI and automations.**

Begin with audit-first reconnaissance and one consolidated Problem Register across:

- provider adapters and effect receipts;
- inbox durability, idempotency and replay;
- AI authority, bounded context and failure behavior;
- automation scheduling, recovery and exact shop/actor binding;
- the retained installed-WebView proof limitation above.

Issue #164 remains the Phase 0–9 execution epic. No application version bump,
release publication, Founder acceptance or Stable claim is authorized by this
closure.

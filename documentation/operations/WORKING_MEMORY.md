# SahelFlow working memory

> **Purpose:** Compact resumable execution frontier; never product/architecture/roadmap authority
> **Last updated:** 2026-08-15
> **Protected main:** `5cb7f5040249a540ed635cdea16dc933843b40aa` — PR #260 / published signed Internal.18
> **Founder-installed Internal.18 result:** **REJECTED / PARTIALLY IMPROVED**
> **Current source frontier:** `agent/internal19-product-convergence` / PR #262 / Internal.19
> **Active product phase:** Phase 6 — Arabic, RTL and accessibility parity
> **Retained evidence:** #221, #226, #230

GitHub live ref owns the transient PR head SHA. Resolve PR #262 head live before every write/review/merge. **No Internal.19 release authority exists yet.** Latest package authority remains Internal.18 / `1.0.0-internal.18` / MSI `1.0.0.18` / FD-037 / `founder-offline-only`.

## Exact resumable frontier

Do **not** restart generic reconnaissance. The repository/layers/docs/current issues are already mapped. Continue only from the current PR #262 correction and exact evidence.

Prior diagnostic exact head `76eda3fb21d58fad1400c1c9df0f8de22071d89b` established:

- general CI + authority/docs + TypeScript/ESLint/complete Vitest/dependency audit/migrations: green;
- Required PR gate: green;
- retry-free Phase 5 static, fresh-install and representative LTR/Arabic RTL workbenches: green;
- Phase 5 artifact manually inspected with no retry/error residue;
- Phase 6–7 static and independent source-quality lanes: green;
- retry-free Phase 6–7 browser: **12 passed / 1 failed**;
- the only remaining failure was the controlled performance trend at **8109 ms** against the unchanged `<8000 ms` CI ceiling.

That old timing case used one sample for each of six different routes, then called the maximum value “p95”; with six values the p95 was simply one worst hosted-runner sample. It also failed to persist the detailed route timing JSON into the uploaded `.sf-inventory` performance directory.

## Final hardening carried by the next PR #262 head

One atomic source/evidence/documentation batch closes the remaining demonstrated gaps:

1. **Locale reconciliation** — keep the client shell/geometry immediately correct, occlude only stale server-rendered `#main-content` while `router.refresh()` reconciles, then reveal the new server tree when `ServerLocaleProvider` clears the pending state before paint.
2. **Next route-transition integration** — declare the observed smooth-scroll behavior on `<html>` as required by Next 16, eliminating the runtime warning seen in the Phase 6–7 job.
3. **Focus-mode semantics** — DashboardLayout owns one screen-reader `<h1>` for Storefront first-run/saved Studio focus routes without touching authorization/data loaders.
4. **Storefront Add Section** — replace hand-rolled absolute popup with the existing Radix-backed DropdownMenu so keyboard open, arrow navigation, Escape dismissal and focus restoration use one shared primitive.
5. **Storefront E2E** — assert focus-mode level-one headings and Add Section keyboard/Escape focus restoration while preserving rich authoring/autosave/reload proof.
6. **Founder visual evidence** — keep first-run Arabic Storefront evidence and additionally capture the real saved Arabic Studio with measured controls/preview/inspector proportions.
7. **Performance trend** — preserve the same 8 s route / 2 s search CI ceilings, measure three samples per route under 4× renderer CPU throttling, gate on the worst persistent route median, retain search p95, and write complete evidence to `.sf-inventory/phase7-performance/browser-trend.json` before assertions.
8. **Retry discipline** — Phase 6–7 remains explicit `--retries=0`; the obsolete one-sample timing case is excluded and replaced by the dedicated repeatable performance spec.
9. **Documentation truth** — current docs and `verify-current-frontier.ts` now distinguish PR #262/Internal.19 source work from the still-authoritative Internal.18/FD-037 release package.

No Golden COD, money/inventory, licensing/native, provider durability, backup/recovery or Storefront publish/checkout authority is intentionally changed by this hardening.

## Protected continuity

Preserve:

- Golden COD command/idempotency/version/audit/event/outbox authority;
- identity, permissions, ShopContext/session and protected-field boundaries;
- append-only money/inventory truth;
- provider durability/reconciliation and AI proposal-bound mutations;
- backup/recovery/migrations/native runtime containment;
- Storefront private draft → exact durable publish/pause/rollback and server-authoritative checkout;
- updater signing/version/release-source protections.

Installed UX rejection never authorizes weakening these boundaries.

## Release boundary

FD-037 is historical/executed Internal.18 release authority. PR #262 is source correction only.

**No Internal.19 release authority exists yet.** After source merge, a newer explicit Founder decision is required before changing version/release request/Cargo/Tauri/native allowlists or dispatching a new signed updater release.

## Exact next actions

1. Re-fetch PR #262 and confirm the live branch head is the new atomic hardening commit; do not use the historical `76eda3fb...` SHA as current evidence.
2. Let the new exact head run general CI/Required PR, retry-free Phase 5 and retry-free Phase 6–7 once.
3. If any lane is red, diagnose the deterministic root cause from logs/artifacts; do not rerun it away.
4. Inspect Phase 5 rendered evidence: four neutral dark presets, loaded Arabic Inbox, loaded Arabic AI, Storefront first-run, **saved Arabic Studio**, Arabic Analytics, and no retry/error residue.
5. Inspect `.sf-inventory/phase7-performance/browser-trend.json`: per-route samples/medians, worst route median, indexed-search p95 and renderer metrics must be present.
6. Obtain/fetch exact-head review and resolve every actionable thread.
7. Merge PR #262 only with expected-head discipline after all required exact-head gates are green.
8. Verify protected-main merge/tree identity.
9. Stop before a new signed package until explicit newer Founder release authority exists.
10. After that decision, make one bounded version/release-authority follow-up, publish only from exact protected main, then perform Founder-installed #221 acceptance.
11. Keep #226 representative installed T470/floor/eight-hour performance/reliability and #230 customer-online Algerian network licensing evidence separate.

## Hard rules

- One active application writer.
- No generic re-audit loop.
- No cross-SHA evidence mix.
- No retry-away of deterministic failure.
- No repeated MSI/full-release build after tiny edits.
- No Founder acceptance claim from automation.
- No customer-online/Beta/Stable claim from FD-037.
- No force/waiver/protection bypass.
- No weakening Rust `--locked`, updater keys, deterministic release-source guards or canonical business authority for convenience.

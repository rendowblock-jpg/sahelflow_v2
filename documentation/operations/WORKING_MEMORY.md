# SahelFlow working memory

> **Purpose:** Compact execution frontier; never product, architecture or roadmap authority
> **Last updated:** 2026-07-31
> **Protected main:** `522ab1642545803c7a9b6c320fe72cceb320e558`
> **Published executable source:** `fb32faedc5ecfc1718e395824f437b805cbb9ef2`
> **Published release:** `1.0.0-internal.13` / MSI `1.0.0.13`
> **Founder-accepted baseline:** Internal.5
> **Operating authority:** FD-028, `../system/ROADMAP.md`, `WORKFLOW.md`, root `AGENTS.md`
> **Execution epic:** issue #164
> **Active phase:** Phase 2A — durable local identity and session authority
> **Active branch:** `agent/phases1-4-completion-program`
> **Active PR:** draft PR #195 — `program: complete phases 1–4`
> **Merge state:** open, draft, mergeable, unmerged
> **Phase 1 closure head:** `3783028396f3b0c4afa43f33fdd3c1c6cc51789f`
> **Phase 1 closure CI:** `30652282305` — success
> **Phase 1 closure source checkpoint:** `30652282191` — success

GitHub PR #195 and its exact current head/checks are the live branch authority.
Never trust copied head or run numbers without re-reading GitHub at session start.

## Protected truth

Phase 0 remains complete. Protected `main`, the published release, installed
Internal.13 evidence and Founder acceptance are unchanged. Draft PR #195 is
proposed source only: it is not merged, published, installed, provider-certified,
externally reviewed or Founder-accepted.

## Phase 1 result — source-closed on the integration branch

The broad Golden COD source boundary and preservation evidence remain as recorded
in PR #195. A fresh sole-agent adversarial pass was used because the Founder is no
longer working with a second coding agent. That pass was explicitly not called
independent review.

It found and closed additional courier P1s involving repeated reconciliation on a
reused delivery, unreadable expired post-effect payloads and pre-query authority
ordering. The consolidated repair proved:

- versioned later booking generations with original-key replay;
- same-generation concurrency exclusion;
- generation-safe repeated reconciliation;
- terminal known-failure recovery;
- missing-tracking ambiguity without retry;
- corrupt pre-effect dead letter and queue isolation;
- unreadable post-effect lease recovery into a real reconciliation state;
- seller authority resolution before raw command replay lookup;
- exact public façade isolation.

Normal CI `30652282305` and Integration source checkpoint `30652282191` both
passed on exact closure head `3783028396f3b0c4afa43f33fdd3c1c6cc51789f`.
Phase 1 is not to be reopened without new concrete P0/P1 evidence.

Production native all-shop restore remains Phase 4. Full Windows/Rust/MSI,
installed, live-provider, legal, Beta and Founder-acceptance evidence remain later
separate gates.

## Active package — Phase 2A session and setup authority

The first Phase 2A vertical is intentionally bounded and migration-free because
the current `Session` model already stores `issuedAt`, `lastSeenAt` and
`revokedAt`.

The package owns:

- setup mode is onboarding only and cannot satisfy authentication;
- exact public auth routes, with auth administration protected by default;
- 24-hour overall session freshness and one-hour inactivity timeout;
- five-minute throttled activity persistence;
- fail-closed timestamp, database and revocation handling;
- successful PIN reauthentication rotates the session identity;
- high-risk callers can require recent PIN proof;
- PIN change revokes all active sessions and establishes one replacement;
- current durable logout semantics remain stronger than obsolete PR #186;
- proxy, route and authority tests cover exact boundaries.

The adopted policy follows current NIST SP 800-63-4 session guidance and OWASP
session-ID rotation guidance. PR #186 is mined only for compatible ideas and is
never merged wholesale.

### Exact next-session order

1. Re-read PR #195, this file and the Phase 2 exit gate.
2. Inspect the exact current Phase 2A source diff and any running checks.
3. Close concrete TypeScript, lint or test diagnostics from that exact head.
4. Add route-level database integration proof for reauthentication and PIN-change
   session rotation if not already covered by the full suite.
5. Run one exact-head source checkpoint for the coherent Phase 2A session package.
6. Continue to durable Person, Workspace, Installation, Member and Device
   authority only after this session/setup boundary is green.
7. Keep PR #195 draft, unmerged and version-neutral.

## Phase 2A next identity kernel package

After the session/setup boundary is green:

1. define durable Person, Workspace, Installation, WorkspaceMember and Device
   authority in the protected installation/control store rather than duplicating
   identity independently in every shop database;
2. bind the compatibility owner to one real owner member and enrolled device;
3. persist policy version and revocation epoch;
4. bind one representative read and one consequential mutation to person, member,
   device, session, installation and exact shop context;
5. prove immediate session/device/member revocation, stale-policy rejection,
   same-shop and cross-shop isolation, restart, duplicate and concurrency behavior;
6. retain PIN only as local unlock and reauthentication, never durable person
   identity.

Licensing, invitations/teams and native multi-shop lifecycle remain later coherent
Phase 2 packages after the identity contract is stable.

## Protected local boundaries

- Do not modify, reset or delete the original checkout solely to make branch work
  easier.
- Preserve `C:\Users\DMR\Desktop\sahelflow_v2\scripts\Founder-install-result.json`.
- Preserve the unrelated modified
  `src/lib/identity/__tests__/session-authority.test.ts` in the original checkout.
- PR #186 (`agent/phase2-session-freshness`) is obsolete/conflicting proposed
  source. Mine it deliberately against current contracts; never merge it directly.
- Keep PR #195 draft and unmerged. Do not bump the application version.

## Validation model

Draft PR #195 runs the targeted exact-head source checkpoint. Keep CI read-only.
Windows standalone, Rust parity, signed MSI, installed lifecycle, live-provider,
independent security/privacy/Law 18-07 and Founder acceptance are later separate
gates.

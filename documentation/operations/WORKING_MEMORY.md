# SahelFlow working memory

> **Purpose:** Compact execution frontier; never product, architecture or roadmap authority
> **Last updated:** 2026-08-08
> **Protected base at package start:** `d511080e0f7c4c951e76c99690d9b7f3b3a03e12`
> **Latest application-changing protected merge:** PR #220
> **Phase 5 product baseline:** `cf6bd90db27b3832c860a7c848ce3a0b8e5a3734`
> **Published executable source:** `fb32faedc5ecfc1718e395824f437b805cbb9ef2`
> **Published release:** `1.0.0-internal.13` / MSI `1.0.0.13`
> **Founder-accepted baseline:** Internal.5
> **Execution epic:** issue #164
> **Retained evidence:** #201, #214, #221
> **Active product phase:** Phase 6 — Arabic, RTL and accessibility parity
> **Active implementation branch:** `agent/phase6-7-completion`
> **Active implementation PR:** #223 — integrated Phase 6→7 source/evidence program; not merge-ready at this checkpoint
> **Source diagnostic checkpoint before documentation-only continuation commits:** `7de771e52affa8d938c44d0d3a06da68cc7c3204`

Live GitHub is authority. Re-fetch protected `main`, PR #223, current issues and
Actions before relying on a SHA. Documentation-only continuation commits may move
the PR head beyond the source diagnostic checkpoint above without changing the
recorded application failure set.

## Phase 5 closure snapshot

Phase 5 is closed at the protected-source + controlled-browser layer through PR
#220 / issue #208. The application-changing protected baseline remains
`cf6bd90db27b3832c860a7c848ce3a0b8e5a3734`.

The exact PR #220 source head passed the Required PR gate, TypeScript, ESLint,
complete Vitest, Prisma/migration checks, production dependency audit and the
Phase 5 Experience Gate. Phase 5 is therefore a protected contract, not work to
reopen during Phase 6/7 repair.

Issue #221 remains deliberately retained because controlled browser evidence does
not prove Founder-installed Windows/Tauri visual acceptance. Reconcile it at the
applicable Phase 5→6 installed UI/accessibility checkpoint; do not convert it into
a claim that Phase 5 source/browser closure is incomplete.

## Active Phase 6 checkpoint

PR #223 intentionally integrates Phase 6 shared correctness with Phase 7
measurement infrastructure so the project does not fall into page-by-page fixes
or CI micro-loops. That integrated implementation shape does **not** advance the
active product phase: the product remains in **Phase 6 — Arabic, RTL and
accessibility parity** until the Phase 6 exit evidence is satisfied.

At source diagnostic checkpoint
`7de771e52affa8d938c44d0d3a06da68cc7c3204`, the following fundamentals were
green:

- TypeScript;
- ESLint;
- complete Vitest;
- production dependency audit;
- migration status;
- Phase 5 Experience Gate.

No collected evidence requires reopening Phase 1–5 architecture, business truth,
permissions, provider durability, recovery or the Phase 5 source/browser
baseline.

## Frozen failure set at the stop checkpoint

The current Problem Register is finite and grouped by shared cause. Do not turn
these into independent drip-fix loops.

1. **Documentation authority continuity.** `sf-audit` failed because this working
   memory had lost the exact `Phase 5 closure snapshot` and `Phase 6 next action`
   continuity markers and no longer exposed the canonical active Phase 6 field.
   This documentation checkpoint restores those contracts. The next session must
   verify the authority gate on the new head before considering this item closed.
2. **Source-quality aggregate contract.** TypeScript, ESLint, complete Vitest,
   production dependency audit and migration status each passed, while the
   aggregate `Require complete source-quality set` assertion still failed. Treat
   this as a gate/aggregation contract defect until its exact condition is
   reconciled; do not re-fix already-green source checks.
3. **Static localization/RTL/accessibility contract.** The integrated Phase 6
   static verifier remains red. Re-fetch the exact current-head findings before
   editing because older literal inventories from prior diagnostic heads are not
   authoritative.
4. **Phase 7 query/index precondition.** Hot-query index/planner verification
   failed before Chromium installation/execution. Therefore no current browser
   product failure is established by that run. Inspect the exact planner contract
   and intended query/index property before changing either code or verifier.

The Required Phase 6-7 completion gate and Required PR gate are red
consequentially because upstream selected gates are red. PR #223 is structurally
mergeable in GitHub but **must not be merged** at this checkpoint.

## Combined Phase 6 → 7 execution contract

Founder direction is to finish the source/evidence program as one coherent
implementation outcome, not a sequence of page fixes and CI-driven micro-PRs.
Issue #164 owns the consolidated Problem Register and shared-contract discipline.

The operating sequence remains:

```text
freeze current PR head
→ verify documentation authority continuity
→ reconcile the complete source/static/query failure set
→ one consolidated dependency-correct repair batch
→ one exact-final-head selected gate rerun
→ Phase 6 source/browser closure
→ applicable native/installed evidence checkpoint
→ Phase 7 installed low-end certification
```

Phase 6 correctness is established first inside the shared branch. Phase 7 may
measure and optimize only that final accessible/RTL surface. Performance work may
not remove accessibility, durable workers, canonical state, recovery or Phase
1–5 authority merely to improve a timing number.

The branch already contains the intended shared-root program: unified
server/client translation fallback authority, shared Sheet/EntityTimeline
localization, RTL submenu direction, route-transition focus entry, compact target
floors, existing Arabic typography/bidi and reduced-motion reuse, bounded intent
prefetch, generalized Phase 6/7 evidence, and hot-query/planner/performance
evidence infrastructure. Those implementations remain subject to the red gates
above; their existence is not itself closure evidence.

## Phase 6 next action

At the start of the next session, do this in order:

1. re-fetch PR #223 and freeze the **current** head; if the branch moved, that head
   supersedes the source diagnostic checkpoint for all new work;
2. verify `sf-audit` / documentation authority on that head and confirm the
   continuation-marker defect is closed;
3. fetch the exact current logs for the source aggregate, static Phase 6 contract
   and hot-query planner precheck, then classify genuine product defects versus
   verifier/gate-contract defects and consequential failures;
4. perform **one consolidated repair batch** across those shared causes only — no
   Phase 1–5 reopening and no one-assertion-at-a-time commits;
5. rerun the selected gates on the exact repaired head in dependency order:
   authority/docs → static locale/RTL/accessibility → source diagnostics →
   query/index + Chromium evidence → required gates;
6. do not merge until those selected source/browser gates are truly green and the
   remaining installed evidence is explicitly retained rather than fabricated.

## Evidence that remains external to source completion

Controlled Chromium is not installed Windows/Tauri certification. Preserve these
as explicit checkpoint obligations after source/browser completion:

- native-Arabic human language/reading review;
- signed installed Windows/Tauri observation;
- issue #221 Founder visual acceptance reconciliation;
- T470 ≤8 s launch / ≤700 ms navigation / ≤350 ms search / ≤500 ms local mutation
  p95;
- declared-floor SSD/HDD and 4 GB evidence;
- eight-hour stability/resource evidence with no sustained memory growth.

Those are evidence/checkpoint actions, not permission to restart an indefinite
coding loop.

## Hard rules

- one active implementation agent and one integrated branch/PR;
- no direct protected-main edits;
- no Phase 1–5 authority weakening;
- active product phase stays Phase 6 until its exit evidence is satisfied;
- no performance optimization without measured evidence;
- re-fetch exact-head failures before editing; old diagnostic inventories are
  leads, not current truth;
- collect the complete failure/review set before repair;
- one consolidated repair batch after the full result, not one fix per workflow
  assertion;
- controlled Chromium evidence must remain separate from installed T470/floor
  certification;
- no release/Beta/Stable/Founder-acceptance claim without matching evidence.

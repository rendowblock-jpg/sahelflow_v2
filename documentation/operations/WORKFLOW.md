# SahelFlow — Research, coding, review and delivery workflow

> **Status:** Active operating contract
> **Last consolidated:** 2026-08-23
> **Governing authority:** FD-028/FD-029 completion model plus newer numbered Founder decisions, current product/experience/architecture contracts and exact protected release authority
> **Execution model:** one active implementation agent; audit-first; batch remediation; tiered CI
> **Current checkpoint:** Internal.24 / FD-043 is signed and published from `main@1037f125188cd93db992979090e156bf787fb54a`; the current outcome is Founder-installed human inspection under #221

Speed comes from accurate scope, root-cause grouping, targeted fast feedback and one frozen certification head—not from weakening integrity, Arabic/RTL, accessibility, security, recovery, performance or evidence honesty.

## 1. Session sequence

Every session begins from live GitHub truth, not a copied documentation SHA.

```text
resolve protected main + open PR/issues + current signed release
→ read active authority
→ identify the one current observable outcome
→ if implementation is needed, map affected authority/consumers/tests/data/evidence
→ research only where a material decision is uncertain or current external authority is required
→ implement one dependency-correct package
→ targeted checks during coding
→ freeze exact head
→ consequence-selected source/browser/native/Windows evidence
→ adversarial review / finding closure
→ expected-head merge
→ verify protected-main durable tree
→ reconcile current truth
→ if separately authorized, build/sign/publish only from exact protected main
→ perform installed/human/provider/external evidence that automation cannot prove
```

At the current frontier, **no implementation or release package is automatically required**. Internal.24 is already published. The immediate task is Founder-installed inspection. A new branch is opened only if the Founder demonstrates a concrete defect or explicitly authorizes a later release/promotion package.

Do not restart generic repository reconnaissance when a bounded current outcome is known. Do not rerun an unchanged passing exact head. Do not retry deterministic failures until they turn green without identifying/fixing the cause.

## 2. Authority and anti-drift

Authority precedence:

1. newer explicit Founder decision for the choice it changes;
2. Product contract;
3. Experience/capability/journey contract;
4. Architecture and protected invariants;
5. exact source/current-state/evidence truth;
6. Roadmap;
7. this Workflow;
8. Working Memory;
9. Research/archive.

Operational rules:

- protected `main` is integrated source truth;
- one active application/release writer owns a product/runtime/release branch at a time;
- no direct application edits on protected `main`;
- Current State owns merged/release/non-claim truth;
- Working Memory owns the compact resume point;
- no important product/release decision remains only in chat;
- issue bodies can retain evidence/work queues but do not replace active documentation authority;
- source, signed artifact, hosted CI install, Founder install, live-provider evidence, representative beta and Stable are distinct truth levels.

Current signed authority is Internal.24 / `1.0.0-internal.24` / MSI `1.0.0.24` / FD-043 / `founder-offline-only`. No Internal.25 or FD-044 exists merely because work continues.

## 3. Complete phase/package audit

Before a major phase/package or a materially uncertain bounded repair, perform the smallest **Complete phase/package audit** that makes the change safe. Inspect every applicable:

- caller and route;
- component/shared primitive;
- service/domain authority;
- permissions/trusted actor/shop/session boundary;
- database model/query/migration;
- protected-field/encryption/index projection;
- native/Tauri/runtime/process boundary;
- provider/outbox/reconciliation path;
- backup/restore/replacement-install consequence;
- AR/FR/EN and RTL/bidi surface;
- responsive/accessibility/state behavior;
- performance/resource hot path;
- direct/contract tests;
- release/version/licensing consequence.

For a broad package, freeze one **Phase Problem Register** before implementation so duplicate manifestations group under root causes and P0/P1 are visible. For one demonstrated installed defect, the reproduction plus bounded impacted-sibling inventory can serve as the register; do not create bureaucracy for its own sake.

At the current Internal.24 checkpoint, do not perform a Complete phase/package audit until Founder inspection actually demonstrates something to repair.

## 4. Research rule

Use current primary/official sources before a material choice that depends on changing standards, platform/provider behavior, law, security guidance or external APIs. Compare alternatives across correctness, security/privacy, recovery, Arabic/RTL/accessibility, performance, maintainability and continuity economics.

Research is bounded. Once sufficient evidence exists for a safe SahelFlow-specific decision and measurable acceptance, implementation begins. Do not create a new permanent masterplan for each problem.

## 5. Targeted implementation checks

Use the cheapest checks that can catch the changed boundary quickly:

- TypeScript and ESLint for TS/React work;
- targeted Vitest/domain/database tests;
- targeted Playwright for changed seller journeys, AR/FR/EN, RTL, keyboard, resize/state transitions;
- provider contract/conformance tests for courier/WhatsApp/AI/cloud changes;
- Prisma generation/validation/migration checks where data authority changes;
- Rust format/check/tests for native changes;
- focused bundle/query/performance measurement for hot paths;
- exact PowerShell parsing/contracts for Windows installer/evidence scripts;
- `bun run sf-version` and updater-contract checks immediately for release-authority work.

Targeted checks are fast feedback, not permission to skip the frozen consequence-selected gate before merge.

## 6. Evidence levels

### Level 1 — Task Gate

Run after a coherent completed task or repair batch. Applicable checks include:

1. complete diff/repository cleanliness;
2. `bun run sf-version`;
3. `bun run sf-audit`;
4. Prisma generation/migration validation where applicable;
5. TypeScript;
6. ESLint;
7. targeted plus required complete Vitest/source suite;
8. production dependency audit where applicable;
9. targeted browser journeys;
10. provider contracts;
11. Rust/native checks;
12. focused Windows/package proof when consequence requires it.

The gate must surface the complete actionable failure set. Do not intentionally hide later failures behind a first-failure-only loop when normal diagnostics can report them safely.

### Frozen review and batch repair

After a coherent implementation package, freeze one exact head and perform adversarial review on that head. Batch related P0/P1 fixes sharing the same root/evidence consequence, then rerun the selected gate set once on the repaired frozen head. Do not bounce through a long chain of tiny review-fix-review commits when one coherent batch can close the dependency safely.

### Level 2 — Phase Checkpoint

Use when a subsystem/broad product boundary is declared source-complete or a later layer depends on the integrated result.

Typical requirements:

- clean frozen dependency install;
- version/documentation authority;
- complete type/lint/unit/domain/integration/database suites;
- migration status/compatibility where applicable;
- production frontend build;
- affected Playwright journeys;
- permissions/revocation/shop isolation;
- duplicate/concurrency/restart/recovery cases;
- AR/FR/EN, RTL/bidi, accessibility/reflow/reduced-motion evidence;
- performance/resource measurements;
- applicable native/Windows/package lanes;
- unresolved-problem/legacy/stale-document audit;
- exact evidence bundle/recommendation.

A subsystem does not close because commits merged or test count is high. The defined seller/Founder outcome and all applicable P0/P1 must be proven.

### Level 3 — Major Full Checkpoint

Use for coherent signed Internal candidates and boundaries where failure can invalidate durable installed authority, including licensing/commercial authority, identity/authorization/cryptography, installer/updater/process supervisor, schema/migration authority, backup/restore/replacement install, destructive lifecycle or irreversible money/stock/provider effects.

Applicable proof includes:

1. exact frozen/reviewed source binding;
2. clean release compilation;
3. Rust release parity;
4. MSI/updater contract/signature;
5. clean install;
6. in-place upgrade/replacement behavior;
7. first launch/authenticated readiness/close/reopen;
8. process containment/cleanup;
9. AppData/registry/database/key preservation;
10. migration interruption/repair;
11. backup/corruption/restore/replacement recovery;
12. complete affected UI journeys;
13. AR/FR/EN/RTL/accessibility evidence;
14. permission/shop isolation;
15. applicable reference-hardware/resource evidence;
16. deterministic evidence manifest.

A signed release is built only after exact protected-main authority exists. Branch-only artifacts are evidence candidates, never durable release sources.

Internal.24 is the current example of a completed Level 3 release path: PR #296 exact head passed CI `32650854784`, Phase 5 `32650854653`, Phase 6-7 `32650854564`, Native source `32650854556`, then protected-main signed run `32669458022` passed and published on attempt 1.

## 7. Whole-product AAA frontend program

The **Whole-product AAA frontend program** is the historical program that produced the shared semantic RTL foundation and later Inbox/AI/Settings/analytics/Inbox-V3/Search/Risk/Automations reconstruction line. It is not automatically reopened by Internal.24.

For any material UI repair later, prove actual changed states rather than component existence:

- happy/loading/empty/filtered-empty/permission/offline/degraded/stale/pending/conflict/error/retry/recovery states as applicable;
- AR/FR/EN semantic parity;
- Arabic joining/typography/reading flow;
- semantic RTL child order/logical geometry;
- portaled primitive direction;
- mixed Arabic/Latin/numeric technical bidi isolation;
- keyboard/focus entry-return-trap and accessible names/status/errors;
- 100–200% zoom/reflow and 1366×768 containment;
- reduced motion;
- live resize where composition changes;
- low-end rendering/interaction budgets.

Shared direction/geometry/bidi contracts from #273–#276 are protected. Do not return to page-local physical-side patching without a demonstrable exceptional geometry contract.

Analytical chrome follows locale direction while Cartesian chronology/quantitative geometry follows governed analytical semantics; do not naively mirror data axes because the UI is Arabic.

## 8. Performance and reliability

Issue #226 is closed, but its budgets remain regression criteria. Performance work remains measurement-driven.

When a target regresses:

1. record representative samples and trace/stage evidence;
2. identify the largest measured root cause;
3. repair that bounded cause;
4. preserve canonical correctness/accessibility/recovery;
5. rerun affected metrics and consequence-selected regressions on the exact repaired head.

Do not raise thresholds merely to remove red. Do not add speculative caches/indexes/virtualization/worker deferral without measured evidence.

The previously observed low-end startup duration is currently deferred by Founder choice; reopen it only on explicit request or new binding evidence.

## 9. Review and repair

Before merge of material work:

1. author self-review;
2. freeze exact head;
3. complete one adversarial review across affected layers;
4. classify findings P0–P3;
5. repair dependency-correctly as one coherent set;
6. re-review materially changed/unresolved areas;
7. rerun affected evidence plus required final gate.

- **P0:** active data loss/exposure/cross-shop/cross-tenant/corrupt recovery/irreversible money-stock damage.
- **P1:** required journey/authority/recovery/provider/major experience/RTL/accessibility/performance defect blocking the intended checkpoint.
- **P2:** bounded hardening.
- **P3:** polish.

Do not merge known applicable P0/P1. A release/package/security P2 may also be blocking when it undermines the authority being certified.

## 10. Current Founder-installed acceptance workflow

Issue #221 owns the current human gate.

For Internal.24:

1. install/update in place on the real Founder Windows machine;
2. preserve AppData/registry/install identity/keys/shop databases;
3. verify `1.0.0-internal.24` / MSI `1.0.0.24` and normal close/reopen;
4. inspect representative whole-product flows;
5. directly validate #293 chart fixes and #294/#295 Automations in EN/FR/AR;
6. inspect Arabic RTL/bidi, typography/density, locale/theme transitions, keyboard/focus/reduced-motion and seller usability;
7. record explicit accept/reject under #221.

CI/signed-workflow installed evidence cannot close this step. If rejection identifies a concrete P0/P1, start one bounded repair workflow from that evidence. If accepted, reconcile the human gate before any later promotion.

## 11. Release/publication discipline

For any later explicit release authority:

- establish a new exact version/decision pair in a reviewed release envelope;
- never infer Internal.25/FD-044 from time or documentation;
- freeze exact release head;
- run selected source/native/Rust/Windows/MSI/install evidence;
- require exact-head review and Required PR gate;
- use expected-head merge;
- re-fetch protected main/tree;
- build/sign/publish only from exact protected main;
- verify signature, install/reopen/hydrated UI, deterministic rewrites, manifest, `latest.json`, tag and publication target;
- keep Founder-installed acceptance and customer-online/provider evidence separate.

## 12. External/provider boundaries

- #230 remains open P1 for resilient customer-online trial/network readiness on representative Algerian networks.
- Real-phone WhatsApp QR/link/reopen/outbound/inbound remains separate provider evidence.
- Representative beta, legal/privacy/security, provider certification, incident/recovery and compatibility gates remain distinct before Stable.
- Founder-offline Internal.24 does not authorize customer-online, Beta or Stable.

## 13. Hard prohibitions

- no cross-SHA evidence mixing;
- no retry-away of deterministic red;
- no force/waiver/protection bypass;
- no hidden product changes inside release-authority work;
- no destructive reset of Founder evidence/state for convenience;
- no branch-only signed release;
- no weakening updater/signing/Rust `--locked`/release-source guards;
- no claim that automation equals Founder human acceptance.

"use client";
import type {StorefrontSection} from "@/lib/storefront/studio-sections";
export function SectionTree({sections,selected,onSelect,onMove,onToggle}:{sections:readonly StorefrontSection[];selected:string|null;onSelect:(id:string)=>void;onMove:(id:string,d:-1|1)=>void;onToggle:(id:string)=>void}){return <div className="space-y-1">{sections.map((s,i)=><div key={s.id} className={`flex gap-1 rounded-lg border p-1 ${selected===s.id?"border-primary":""}`}><button onClick={()=>onSelect(s.id)} className="flex-1 truncate px-2 text-start text-xs">{s.type}</button><button disabled={!i} onClick={()=>onMove(s.id,-1)}>↑</button><button disabled={i===sections.length-1} onClick={()=>onMove(s.id,1)}>↓</button><button onClick={()=>onToggle(s.id)}>{s.enabled?"●":"○"}</button></div>)}</div>}

"use client";

import { useMemo, useState } from "react";
import { useRouter } from "next/navigation";
import { AlertTriangle, ChevronDown, ChevronUp, RotateCcw } from "lucide-react";

import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { useI18n } from "@/hooks/use-i18n";
import { toast } from "@/lib/toast";

interface AttemptHistory { id: string; attemptNumber: number; state: string; errorCode: string | null; startedAt: string; completedAt: string | null; }
interface StepHistory { id: string; position: number; action: string; failurePolicy: string; status: string; attemptCount: number; operatorRetryCount: number; nextAttemptAt: string | null; lastErrorCode: string | null; effectKey: string | null; effectState: string | null; attempts: AttemptHistory[]; }
export interface AutomationRunHistoryView { id: string; automationName: string; triggerType: string; status: string; stepCount: number; succeededStepCount: number; failedStepCount: number; skippedStepCount: number; operatorRetryCount: number; nextAttemptAt: string | null; lastErrorCode: string | null; createdAt: string; recoverable: boolean; recoveryBlockCode: string | null; steps: StepHistory[]; }
interface Props { initialRuns: AutomationRunHistoryView[]; canRecover?: boolean; }
function statusVariant(status: string): "default" | "secondary" | "destructive" | "outline" { if (status === "succeeded") return "default"; if (["failed", "dead_letter", "ambiguous"].includes(status)) return "destructive"; if (["queued", "processing", "retrying", "waiting_effect"].includes(status)) return "secondary"; return "outline"; }

export function AutomationRunRecoveryPanel({ initialRuns, canRecover = false }: Props) {
  const { t, locale } = useI18n(); const router = useRouter(); const [expanded, setExpanded] = useState<string | null>(null); const [reasons, setReasons] = useState<Record<string, string>>({}); const [retryingRun, setRetryingRun] = useState<string | null>(null); const formatter = useMemo(() => new Intl.DateTimeFormat(locale, { dateStyle: "medium", timeStyle: "short" }), [locale]);
  const retryRun = async (runId: string) => { if (!canRecover) return; const reason = reasons[runId]?.trim() ?? ""; if (reason.length < 3) { toast.error(t("automations.runtime.retryReasonRequired")); return; } setRetryingRun(runId); try { const response = await fetch("/api/automations/runs/recovery", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ runId, reason }) }); if (!response.ok) throw new Error("AUTOMATION_RETRY_FAILED"); toast.success(t("automations.runtime.retryQueued")); setReasons((current) => ({ ...current, [runId]: "" })); router.refresh(); } catch { toast.error(t("automations.runtime.retryFailed")); } finally { setRetryingRun(null); } };
  return <Card><CardHeader><CardTitle className="text-base">{t("automations.runtime.history")}</CardTitle></CardHeader><CardContent className="p-0">{initialRuns.length === 0 ? <p className="p-4 text-sm text-muted-foreground">{t("automations.runtime.noRuns")}</p> : <div className="divide-y">{initialRuns.map((run) => { const isExpanded = expanded === run.id; return <section key={run.id} className="p-4" aria-labelledby={`run-${run.id}`}><div className="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div className="min-w-0 space-y-1"><div className="flex flex-wrap items-center gap-2"><h3 id={`run-${run.id}`} className="font-medium">{run.automationName}</h3><Badge variant={statusVariant(run.status)}>{t(`automations.runtime.state.${run.status}`)}</Badge></div><p className="text-xs text-muted-foreground">{run.triggerType} · {formatter.format(new Date(run.createdAt))}</p><p className="text-xs text-muted-foreground">{t("automations.runtime.runCounts").replace("{{succeeded}}", String(run.succeededStepCount)).replace("{{failed}}", String(run.failedStepCount)).replace("{{skipped}}", String(run.skippedStepCount)).replace("{{total}}", String(run.stepCount))}</p></div><Button type="button" variant="ghost" size="sm" aria-expanded={isExpanded} aria-controls={`run-details-${run.id}`} onClick={() => setExpanded(isExpanded ? null : run.id)}>{isExpanded ? <ChevronUp className="me-1 size-4" /> : <ChevronDown className="me-1 size-4" />}{isExpanded ? t("automations.runtime.hideDetails") : t("automations.runtime.showDetails")}</Button></div>{run.nextAttemptAt ? <p className="mt-2 text-xs text-muted-foreground">{t("automations.runtime.nextAttempt")}: {formatter.format(new Date(run.nextAttemptAt))}</p> : null}{canRecover && run.recoverable ? <div className="mt-3 flex flex-col gap-2 sm:flex-row"><Input value={reasons[run.id] ?? ""} onChange={(event) => setReasons((current) => ({ ...current, [run.id]: event.target.value }))} maxLength={500} aria-label={t("automations.runtime.retryReason")} placeholder={t("automations.runtime.retryReason")} /><Button type="button" variant="outline" disabled={retryingRun === run.id} onClick={() => retryRun(run.id)}><RotateCcw className="me-1 size-4" />{t("automations.runtime.retry")}</Button></div> : null}{!run.recoverable && run.recoveryBlockCode && ["dead_letter", "ambiguous", "partially_completed", "failed"].includes(run.status) ? <div className="mt-3 flex items-start gap-2 rounded-md border p-3 text-sm text-muted-foreground"><AlertTriangle className="mt-0.5 size-4 shrink-0" /><span>{run.recoveryBlockCode === "AUTOMATION_EFFECT_RECOVERY_REQUIRED" || run.recoveryBlockCode === "AUTOMATION_EFFECT_AMBIGUOUS" ? t("automations.runtime.effectRecoveryRequired") : t("automations.runtime.retryUnavailable")}</span></div> : null}{isExpanded ? <div id={`run-details-${run.id}`} className="mt-4 space-y-3">{run.steps.map((step) => <div key={step.id} className="rounded-md border p-3"><div className="flex flex-wrap items-center gap-2 text-sm"><span className="font-medium">{t("automations.runtime.step").replace("{{count}}", String(step.position + 1))}</span><span>{step.action}</span><Badge variant={statusVariant(step.status)}>{t(`automations.runtime.state.${step.status}`)}</Badge></div>{step.lastErrorCode ? <p className="mt-2 text-xs text-destructive" dir="ltr">{step.lastErrorCode}</p> : null}{step.effectKey ? <p className="mt-2 break-all font-mono text-xs text-muted-foreground" dir="ltr">{t("automations.runtime.effect")}: {step.effectKey}</p> : null}{step.attempts.length > 0 ? <div className="mt-3 space-y-1"><p className="text-xs font-medium">{t("automations.runtime.attempts")}</p>{step.attempts.map((attempt) => <p key={attempt.id} className="text-xs text-muted-foreground">#{attempt.attemptNumber} · {t(`automations.runtime.state.${attempt.state}`)}{attempt.errorCode ? ` · ${attempt.errorCode}` : ""}</p>)}</div> : null}</div>)}</div> : null}</section>; })}</div>}</CardContent></Card>;
}

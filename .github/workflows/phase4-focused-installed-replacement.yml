name: Phase 4 focused installed replacement

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: phase4-focused-installed-replacement-${{ github.ref }}
  cancel-in-progress: true

jobs:
  installed-replacement:
    uses: ./.github/workflows/windows-installed-e2e.yml

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for SahelFlow main window. The webview does NOT need shell:execute/spawn — the Next.js server and WhatsApp sidecar are spawned at startup by Rust (src-tauri/src/lib.rs spawn_services), not by the webview. Granting shell:execute to the webview would make any XSS in the webview equivalent to arbitrary command execution on the user's machine.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "store:default",
    "os:default",
    "process:default",
    "stronghold:default"
  ]
}
